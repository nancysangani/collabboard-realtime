services:
  - type: web
    name: collabboard-server
    runtime: node
    plan: free
    buildCommand: |
      cd client && npm install && npm run build && cd .. && npm install --prefix server
    startCommand: npm --prefix server start
    envVars:
      - key: NODE_ENV
        value: production
